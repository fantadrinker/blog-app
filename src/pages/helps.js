import React from "react";
import NavBar from "../components/NavBar";

const Helps = (props) => {
    return (
        <div>
            <NavBar />
            <span>
                Need help navigating? Send email to placeholder.name@outlook.com
            </span>
        </div>
    )
}

export default Helps;